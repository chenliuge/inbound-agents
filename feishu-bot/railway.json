{
  "$schema": "https://schema.railway.app/railway-schema.json",
  "deploy": {
    "startCommand": "gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 600 --graceful-timeout 600 --worker-class gthread --threads 4"
  }
}
